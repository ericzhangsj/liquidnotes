/target
# Local copy of the built app that we keep in the folder for easy launching.
*.exe
*.png.tmp

# Personal dev helper with a machine-specific toolchain path (see README for
# the generic build steps).
/run.bat

# Editor / OS cruft
*.rs.bk
.DS_Store
Thumbs.db
