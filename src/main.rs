// liquidnotes — scaffold. Engine lands in the next commits:
//   gpu/device.rs   shared D3D11 device
//   gpu/capture.rs  desktop duplication + background reconstruction
//   gpu/glass.rs    liquid-glass shader pipeline
//   ui/             plus button, note windows, popup menu
fn main() {
    println!("liquidnotes scaffold — engine not built yet");
}
